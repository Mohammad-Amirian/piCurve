% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Suggest_PIpar.R
\name{get_start_piPars}
\alias{get_start_piPars}
\title{Suggest Initial Parameter Values for P–I Curve Fitting}
\usage{
get_start_piPars(df)
}
\arguments{
\item{df}{A data frame containing columns \code{P} (photosynthesis rate) and \code{I} (irradiance).}
}
\value{
A matrix with two rows (\code{par1}, \code{par2}) and three columns (\code{Pmax}, \code{alpha}, \code{beta})
representing candidate starting values for model parameters.
}
\description{
Provides two sets of initial parameter estimates (\code{Pmax}, \code{alpha}, and \code{beta}) for fitting
photosynthesis–irradiance (PI) models. These values are computed based on the raw data
and are useful for initializing nonlinear curve fitting procedures.
}
\details{
The function identifies the maximum photosynthesis rate (\code{Pmax}) and calculates two estimates
of the photochemical efficiency (\code{alpha}) and the photoinhibition term (\code{beta}) based on light
levels where photosynthesis exceeds 90\% of \code{Pmax}. This heuristic approach helps provide
stable starting points for model optimization routines.
}
\examples{
params <- c(Pmax = 20, alpha = 0.6, beta = 0.1, R = 0)
df <- tibble::tibble(I = seq(0, 300, length = 50))
df$P <- Model_piCurve(parameters = c(params, beta = 0.3),
                      model_name = "2tanh",
                      data = df)
get_start_piPars(df)


}
