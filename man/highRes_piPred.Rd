% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HighRes_piPred.R
\name{highRes_piPred}
\alias{highRes_piPred}
\title{Generate High-Resolution Model Predictions for PI Curves}
\usage{
highRes_piPred(Fitted_Model, data, length_out = 50)
}
\arguments{
\item{Fitted_Model}{A fitted model object returned by \code{Fit_piModel()}, containing the parameter estimates and model name.}

\item{data}{A data frame or tibble with at least columns \code{I} (irradiance) and \code{P} (photosynthesis). Used to define the prediction range.}

\item{length_out}{Integer. Number of points to generate across the irradiance range. Default is 50.}
}
\value{
A tibble with high-resolution irradiance values and the corresponding model-predicted photosynthesis rates (\code{Phat}).
}
\description{
This function takes a fitted photosynthesis–irradiance (PI) model and returns
a high-resolution prediction of photosynthetic rate (\code{P}) across a finely spaced
irradiance (\code{I}) range based on the input dataset.
}
\details{
If the input \code{data} does not contain the expected columns \code{I} and \code{P}, the function attempts to format it using \code{FormatCheck_piCurve()}.
}
\examples{
df <- tibble::tibble(
  I = c(0, 33, 67, 100, 133, 167, 200),
  P = c(0, 15, 19, 20, 20, 20, 20)
)

fit <- Fit_piModel(data = df)

pred_highres <- highRes_piPred(fit, data = df)

}
