% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PAR_sea_surface_Wm2.R
\name{PAR_sea_surface_piCurve}
\alias{PAR_sea_surface_piCurve}
\title{Estimate sea-surface photosynthetically available radiation (PAR)}
\source{
Gregg, W. W., & Carder, K. L. (1990). A simple spectral solar irradiance
model for cloudless maritime atmospheres. \emph{Journal of Geophysical Research}.
}
\usage{
PAR_sea_surface_piCurve(
  zenith_angle,
  database_info,
  sample_date,
  H_oz = 266,
  data_climato_solar = NULL,
  lambda_min = 400,
  lambda_max = 700
)
}
\arguments{
\item{zenith_angle}{Numeric scalar. Solar zenith angle in degrees
(\verb{0 <= zenith_angle < 90}). Users may estimate solar zenith angle using
\code{\link[=solar_zenith_angle_piCurve]{solar_zenith_angle_piCurve()}}.}

\item{sample_date}{A date coercible to \code{Date}. Used to compute orbital
correction of extraterrestrial irradiance.}

\item{H_oz}{Numeric scalar. Total column ozone depth in Dobson Units (DU).
Default is \code{266}.}

\item{data_climato_solar}{Optional list or one-row data frame of atmospheric
inputs. If \code{NULL}, default climatological values are used.}

\item{lambda_min}{Numeric scalar. Lower wavelength bound for PAR integration
in nm. Default is \code{400}.}

\item{lambda_max}{Numeric scalar. Upper wavelength bound for PAR integration
in nm. Default is \code{700}.}
}
\value{
A one-row data frame with columns:
\describe{
\item{lambda_min}{Lower wavelength bound of integration (nm).}
\item{lambda_max}{Upper wavelength bound of integration (nm).}
\item{PAR}{Integrated sea-surface PAR (W m\eqn{^{-2}}).}
}
}
\description{
Estimates clear-sky photosynthetically available radiation (PAR) at the sea
surface by integrating spectral irradiance from 400 to 700 nm using
\code{\link[=sea_surface_irradiance_piCurve]{sea_surface_irradiance_piCurve()}}.
}
\details{
This function first estimates spectral downwelling irradiance at the sea
surface using \code{\link[=sea_surface_irradiance_piCurve]{sea_surface_irradiance_piCurve()}}, then integrates irradiance
over the photosynthetically active waveband. By default, PAR is computed over
400--700 nm.

Integration is performed numerically using the trapezoidal rule:
\deqn{
PAR = \int_{\lambda_{min}}^{\lambda_{max}} E_d(\lambda)\ d\lambda
}

The returned PAR is expressed in energy units (\verb{W m^-2}). If quantum units
are needed (e.g., \verb{\\mumol photons m^-2 s^-1}), an additional spectral
conversion from energy to photon flux is required.

The dataset \link{GreggCarder1990} provides wavelength-specific coefficients
consistent with the Gregg and Carder (1990) formulation.
}
\examples{
# Example using Gregg & Carder (1990) dataset
PAR_sea_surface_piCurve(
  zenith_angle = 30,
  database_info = GreggCarder1990,
  sample_date = "2024-07-24"
)

# Restrict integration bounds if needed
PAR_sea_surface_piCurve(
  zenith_angle = 30,
  database_info = GreggCarder1990,
  sample_date = "2024-07-24",
  lambda_min = 450,
  lambda_max = 650
)

# Example based on coordinators & time (unknown zenith angle)
DATE <- as.Date("2024-03-20")
zn <- solar_zenith_angle_piCurve(DATE, hour_utc = 12, latitude = 0, longitude = 0)

PAR_sea_surface_piCurve(zenith_angle = zn, database_info = GreggCarder1990, sample_date = DATE)

}
