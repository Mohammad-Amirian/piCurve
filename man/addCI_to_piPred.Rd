% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addCI_to_piPred.R
\name{addCI_to_piPred}
\alias{addCI_to_piPred}
\title{Add Confidence Interval to PI Predictions}
\usage{
addCI_to_piPred(
  Fitted_Model,
  irrad,
  n_cores = 1,
  n_sample = 1000,
  Clevel = 0.95
)
}
\arguments{
\item{Fitted_Model}{A model object returned by \code{\link{Fit_piModel}} including parameter estimates and model name.}

\item{irrad}{A numeric vector of irradiance (\code{I}) values at which to compute prediction intervals.}

\item{n_cores}{Integer. Number of CPU cores to use for parallel sampling (default: 1).}

\item{n_sample}{Integer. Number of parameter samples to draw from the confidence intervals (default: 1000).}

\item{Clevel}{Confidence level for interval computation (default: 0.95).}
}
\value{
A data frame with columns:
\itemize{
\item \code{irradiance}: input irradiance values
\item \code{ymin}: lower bound of predicted \code{P} at each \code{I}
\item \code{ymax}: upper bound of predicted \code{P} at each \code{I}
}
}
\description{
Generates confidence intervals for predicted photosynthesis values (\code{P}) given a fitted PI curve model.
}
\details{
This function first extracts confidence intervals for each estimated model parameter using \code{\link{ConfInt_piCurve}}.
It then samples \code{n_sample} parameter sets from these intervals and generates predictions for each.
Finally, it computes the confidence interval bounds of predicted photosynthetic rate at each irradiance level.

Parallel computation is used to speed up sampling. The interval is two-tailed and centered based on the requested confidence level.
}
\examples{
params <- c(Pmax = 20, alpha = 0.6, R = 0)
df <- tibble::tibble(I = seq(0, 100, length = 25))
df$P <- Model_piCurve(parameters = params, model_name = "LS5", data = df) +
  5 * rnorm(25, 0, 0.25)

fit <- Fit_piModel(parameters = c(params, StDev = 2), STATapp = "MLE",
                   Hessian = TRUE, data = df)

cal_ci <- addCI_to_piPred(fit, irrad = df$I, n_sample = 500)

df_ci <- tibble::tibble(
  cal_ci,
  .obs = df$P,
  .pred = Model_piCurve(fit$par, fit$model, data = df)
)
plot(df_ci$irradiance, df_ci$.obs, xlab = "Irradiance", ylab = "Photosynthesis Rate",
     ylim = c(min(df_ci$ymin), max(df_ci$ymax)))
lines(df_ci$irradiance, df_ci$.pred)
lines(df_ci$irradiance, df_ci$ymin, lty = "dashed")
lines(df_ci$irradiance, df_ci$ymax, lty = "dashed")

}
