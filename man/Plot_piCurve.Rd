% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plot_piCurve.R
\name{Plot_piCurve}
\alias{Plot_piCurve}
\title{Plot Fitted PI Curve with Optional Confidence Intervals}
\usage{
Plot_piCurve(
  Fitted_Model,
  data,
  add_CI = FALSE,
  n_cores = 2,
  length_out = 50,
  point_size = 2
)
}
\arguments{
\item{Fitted_Model}{A model object returned by \code{\link{Fit_piModel}} containing parameter estimates and model type.}

\item{data}{A data frame containing observed data with columns \code{I} (irradiance) and \code{P} (photosynthesis).}

\item{add_CI}{Logical. Whether to add confidence intervals around the fitted curve (default: \code{FALSE}, see details).}

\item{n_cores}{Integer. Number of CPU cores to use if computing confidence intervals (default: 2).}

\item{length_out}{Integer. Number of points to use when generating the high-resolution fitted curve (default: 50).}

\item{point_size}{Numeric. Size of observed data points in the plot (default: 2).}
}
\value{
A \code{ggplot} object showing the observed data points, fitted PI curve, and optionally the confidence interval bounds.
}
\description{
Visualizes a fitted photosynthesis–irradiance (PI) model along with optional confidence intervals on
observed data points.
}
\details{
If \code{add_CI = TRUE}, the function internally calls \code{\link{addCI_to_piPred}} to compute confidence intervals at each irradiance level used for prediction.

If the input data is not properly formatted (missing columns "P" or "I"), the function attempts to fix it using \code{\link{FormatCheck_piCurve}}.
}
\examples{
df <- tibble::tibble(
  I = c(0, 33, 67, 100, 133, 167, 200),
  P = c(0, 15, 19, 20, 20, 20, 20)
)

fit <- Fit_piModel(
  parameters = c(Pmax = 19, alpha = 0.5, R = 0, StDev = 0.1),
  data = df, STATapp = "MLE", Hessian = TRUE
)

Plot_piCurve(fit, data = df, add_CI = TRUE, n_cores = 2)

}
